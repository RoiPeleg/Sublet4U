<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sublet4u">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sublet4U">
        <activity
            android:name=".addapartmentActivity"
            android:label="@string/title_activity_addapartment"
            android:theme="@style/Theme.Sublet4U.NoActionBar" />

        <activity
            android:name=".FindApartmentUser"
            android:label="FindApartmentUser"
            android:theme="@style/Theme.Sublet4U.NoActionBar" />
        <activity
            android:name=".OwnerActivity"
            android:label="@string/title_activity_owner"
            android:theme="@style/Theme.Sublet4U.NoActionBar" />
        <activity
            android:name=".ui.login.RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.Sublet4U.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>